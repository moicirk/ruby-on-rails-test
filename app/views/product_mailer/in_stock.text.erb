Good news!

<%= @product.name %> is back in stock.
<%= product_url(@product) %>

Unsubscribe: <%= product_unsubscribe_url(token: params[:subscriber].generate_token_for(:unsubscribe),
                                 id: params[:subscriber].id,
                                 product_id: @product.id) %>